```js

// Data
userId = 1;
postId = 1;

// Construct objects (models, controllers)

// Functional Programing
const reqHandler = new RequestHandler(BASE_URL);

// Usage using Functional Programing
const post = await printTargetPost(userId, postId);

const allPost = await printAllPost(userId);



```
